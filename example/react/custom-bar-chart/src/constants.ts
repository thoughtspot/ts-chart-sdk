/**
 * Variable to control v1 to v2 schema conversion visibility.
 * When v1 is converted to v2, we can make all the elements to
 * display section and columnSettings to column section.
 */
export const wantToSeeV1ToV2Conversion = false;
